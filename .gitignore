# Dependencias de Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Entorno local y debug
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivo de base de datos local (opcional, pero para entregas académicas a veces se conserva.
# Aquí lo dejamos sin ignorar para que el docente tenga datos de prueba en data.json, 
# pero ignoramos archivos temporales).

# Sistema
.DS_Store
Thumbs.db

# Directorio temporal de la plantilla
temp_template/
